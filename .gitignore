# Environment & Secrets
.env
.env.local
.env.*.local
credentials/
secrets/
config/google_service_account.json
config/google_token.json
config/google_oauth_credentials.json
client_secret_*.json

# Personal config (contains name, email, phone — use profile.example.yml as template)
config/profile.yml

# Resume markdown fallback (contains real personal contact info)
config/cv.md

# macOS Finder artefact (colon in filename — same file as google_service_account.json)
config:google_service_account.json

# Claude local settings (may contain personal paths or URLs)
.claude/settings.local.json

# Docker
docker-compose.override.yml

# n8n
.n8n/
node_modules/
n8n_storage/

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
.vscode/settings.json

# Generated data (keep templates)
data/*.jsonl
data/*.json
data/*.tsv
data/tailored/
output/
*.json.backup

# But do track:
!data/applications.md
!data/.gitkeep

# Optional: Don't commit workflow exports initially
# workflows/*.json

# macOS
.AppleDouble
.AppleDB
*.swp
*~
